import { Renderer, RendererContext } from "../model/renderer";
import { Node, Edge, Layers } from "../model/graph";

export class SvgRenderer implements Renderer {
  arrowShapes(): void {
  }

  drawingEdges(edges: Edge[]): void {
  }

  drawingElements(context: RendererContext, elements: Layers): void {
  }

  drawingImages(): void {
  }

  drawingLabelText(): void {
  }

  drawingNodes(nodes: Node[]): void {
  }

  drawingShapes(): void {
  }

  exportImage(): void {
  }
}
